version: "3.9"

services:
  discord_bot:
    build: .
    container_name: discord_bot
    working_dir: /app/bot
    command: python main.py
    env_file:
      - .env
    volumes:
      - ./bot:/app/bot
